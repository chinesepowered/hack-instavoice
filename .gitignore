node_modules
.next
out
.env
.env.local
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Vendor tooling writes agent skills, hooks and telemetry state into the repo
# (`insta project create` installs 24 SKILL.md files plus a PostToolUse hook).
# None of it is source, and none of it should reach a commit or an agent's
# context, so it is ignored wholesale rather than per-file.
.insta/
.claude/
.codex/
.agents/
.github/skills/
skills-lock.json
.insta-home/
